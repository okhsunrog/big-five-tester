# Big Five Personality Test - Environment Variables
# ================================================
#
# AI configuration is in ai_config.toml (see ai_config.example.toml)
# Here you only need to set API keys referenced in the config.

# OpenRouter API key (for safeguard, translation, or main analysis)
OPENROUTER_API_KEY=your_openrouter_api_key_here

# Anthropic API key (if using Anthropic directly for analysis)
# ANTHROPIC_API_KEY=your_anthropic_api_key_here

# Optional: Custom path to AI config file (default: ./ai_config.toml)
# AI_CONFIG_PATH=/path/to/ai_config.toml
